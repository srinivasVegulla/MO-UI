# must be unique in a given SonarQube instance
#sonar.projectKey=ECB:RestAPI
# this is the name and version displayed in the SonarQube UI. Was mandatory prior to SonarQube 6.1.
#sonar.projectName=ECB RESTful API Services
#sonar.projectVersion=1.0
sonar.projectKey=ecb-ui-metraoffer
sonar.projectName=ecb-ui-metraoffer
sonar.projectVersion=0.1

sonar.java.coveragePlugin=jacoco

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Since SonarQube 4.2, this property is optional if sonar.modules is set. 
# If not set, SonarQube starts looking for source code from the directory containing 
# the sonar-project.properties file.

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

sonar.jacoco.reportPaths=build/jacoco/test.exec
sonar.junit.reportsPath=build/test-results/test
sonar.sources=src/main,src/app,src/assets,src/environments,src/styles
sonar.java.binaries=build/classes/java/main
sonar.tests=src/test
sonar.projectKey=ecb-ui-metraoffer
sonar.projectName=ecb-ui-metraoffer
sonar.projectVersion=0.1
sonar.exclusions=src/main/resources/static/**
